import React, {Fragment} from 'react';
import Header from '../Header/Header';

const Main = () => {
    return (
        <Fragment>
            <Header></Header>
        </Fragment>
    );
};

export default Main;
